# AI Web 交互优化与治理后台重构路线图

更新时间：2026-07-24

本文记录 AI Web 模块从“能力完整”走向“用户任务闭环完整”的交互优化方案、当前完成状态和推荐实施顺序。AI Web 的长期架构、权限边界、状态与数据流仍以 `AI_WEB_FRONTEND_DESIGN.zh-CN.md` 为事实来源；本文主要回答“接下来优化什么、按什么顺序验收”。

状态标记：

- **已完成**：已有实现和自动化验证证据。
- **部分完成**：主链路已实现，但仍有明确体验或恢复缺口。
- **待实施**：尚未进入实现或当前实现仍不满足目标。
- **待人工验收**：自动化和部署已通过，但需要真实角色与浏览器操作确认。

## 1. 总体判断

AI 模块底层设计保持不变，并继续作为后续优化的基础：

- 权限、公司范围、记录权限和正式写入裁决归 Frappe。
- 业务事实使用结构化 citation，不从 Markdown 或自然语言猜测。
- 草稿具备不可变版本、乐观锁、后端校验和状态机。
- 正式执行具备幂等、审计和持久化业务回执。
- 正式写入复用既有商品、销售、采购和库存领域服务。
- 模型注册、策略、预算、用量、向量、审计和数据任务具备治理边界。

当前主要问题不是重做架构，而是继续收紧 Web 编排和信息层级，减少“能力已经具备，但用户需要在多个入口之间来回切换”的割裂感。

## 2. 目标任务闭环

```text
自然语言请求
  → AI 草稿摘要
  → 草稿工作台
      编辑字段
      保存并实时校验
      查看执行影响摘要
      确认执行
  → Frappe 正式领域服务
  → 原地显示正式业务回执
  → 按需进入完整业务页面处理复杂例外
```

完整业务页面用于处理批次、序列号、复杂明细、审批和其他专业例外，不应成为普通草稿的必经步骤。模型本身不能执行正式写入；只有当前用户明确确认后，Frappe 才能通过正式领域服务执行。

## 3. 当前交付状态

| 能力 | 状态 | 当前事实与剩余缺口 |
| --- | --- | --- |
| 保存草稿后保持工作台打开 | 已完成 | 保存成功后使用后端返回的新版本、payload 和 validation 刷新当前表单。 |
| 工作台内确认执行和原地回执 | 已完成 | 未保存修改会先保存；通过业务复核和二次确认后执行最新版本，并在原编辑器显示回执。 |
| 执行前读取最新持久草稿 | 已完成，待人工验收 | 每次打开和执行前均读取最新版本；版本变化时停止执行并刷新，保存或最终确认发生冲突时进入三方字段对比，可选择重放本地字段。 |
| 草稿入口操作层级 | 已完成 | 会话卡片和草稿中心统一使用“复核并执行 / 完善草稿”，版本历史、业务编辑器和放弃进入更多菜单。 |
| 草稿卡片关键业务摘要 | 已完成，待人工验收 | 会话 citation 和草稿中心共用紧凑摘要；商品展示四档价格与初始库存，销售/采购按 UOM 分组数量并展示完整金额合计，库存调整展示当前、目标和差异。 |
| 失败消息留在消息流 | 已完成，待人工验收 | 失败消息、原问题、场景、模型、稳定错误码和 Run 可从持久数据恢复；按可重试、需修改输入、权限拒绝和系统/治理故障展示不同说明与动作。 |
| 每条回答独立 Run | 已完成，待人工验收 | 成功和失败回答都按消息打开自身 Run；顶部仅在生成中显示“当前运行”，默认概览与高级诊断分层，历史工具过程不复用其他 Run 数据。 |
| 长会话分页与增量加载 | 已完成，待人工验收 | 首次加载最近 40 条消息，使用稳定消息序号游标向前加载；前置历史后保持阅读位置，不覆盖未发送文本或当前 Run，并恢复每页 Run、citation、反馈、失败状态和草稿入口。 |
| 常用 Prompt 先回填再发送 | 已完成 | 点击常用能力只回填 Sender 和场景，不自动产生模型调用。 |
| 归档会话只读 | 部分完成 | Sender 已禁用并提供新建会话入口；复制上下文新建或恢复会话仍待评估和实现。 |
| staging 登录后完整任务验收 | 待人工验收 | 镜像、路由和 HTTP 健康已验证；仍需真实测试账号验收草稿、回执、归档和失败恢复。 |

当前编辑容器仍为 `AiDraftEditorModal`。统一任务闭环已经形成；是否迁移为右侧大 Drawer，应以真实浏览器验收中的对话上下文保留、字段密度和响应式表现为依据，不把组件形态本身当作完成标准。

## 4. P0：草稿工作台闭环

### 4.1 统一编辑、校验、复核和执行

草稿工作台统一承载：

- 顶部：草稿类型、状态、版本和最近校验时间。
- 主体：对应商品、销售、采购或库存调整的业务编辑表单。
- 复核区：后端校验、错误、警告和本次执行影响摘要。
- 固定操作区：取消、保存草稿、更多操作和确认执行。
- 更多操作：版本历史、进入完整业务编辑器、放弃草稿。

确认执行必须遵循：

1. 检查是否存在未保存修改。
2. 如有修改，先保存并取得后端返回的新版本。
3. 重新读取或确认最新持久版本，并使用后端结果校验。
4. 校验失败时保持工作台打开，展示字段和业务错误。
5. 校验通过后展示最终影响摘要并要求二次确认。
6. 使用最新版本执行，执行期间禁止关闭、重复保存或重复提交。
7. 成功后原地切换为正式业务回执。

后端版本锁、状态检查、幂等、权限和审计继续由 `apps/myapp/myapp/services/ai_service.py` 负责，Web 不复制这些状态机规则。

### 4.2 执行前读取最新草稿

任何确认执行入口都必须先调用领域 Service 读取最新草稿，而不是直接使用消息 citation、列表行或浏览器内存中的旧快照。

当前实现：

- 保存或最终执行收到版本冲突时自动进入“版本冲突对比”；无本地修改的执行前刷新若发现新版本，则刷新表单并要求用户重新确认，不直接执行。
- 保留用户尚未保存的输入，并按字段展示“原打开版本 / 我的输入 / 最新持久版本”。
- 支持全部接受最新版本，或在最新持久版本上选择性重放本地修改；订单商品明细作为一个整体冲突字段处理，不按数组下标静默合并。
- 保存、历史恢复和执行的稳定冲突契约为 HTTP `409` 与 `code=AI_DRAFT_VERSION_CONFLICT`；Web 只把旧中文错误文案作为临时兼容，不作为正式协议。
- 最终确认摘要只能基于最新持久版本生成。

上述自动化已完成，仍需在 staging 使用两个页面并发编辑同一草稿，人工验收字段选择、焦点、横向表格和最终执行回执。

确认摘要至少包括：

- 商品：名称、库存单位、四档价格、初始库存和仓库。
- 销售/采购：往来单位、日期、行数、数量、含税金额和仓库。
- 库存调整：当前库存、目标库存、差异数量、估值参考和原因。

### 4.3 失败消息与恢复

请求失败后必须保留消息位置和 Run，不得只显示 Toast：

- 显示失败阶段和用户可理解的原因。
- 提供重新发送和查看诊断。
- 页面刷新后可从 Run 恢复。
- 手动重试复用原问题、场景、公司和模型上下文。
- 不自动重复可能产生费用的模型调用。
- 已返回的结构化业务结果不因后续摘要失败而消失。

当前已按稳定错误码区分恢复动作：

- 网络中断、临时服务不可用、限流、并发和模型熔断：保留上下文，提供“稍后重试”，不自动调用模型。
- 请求校验或模型拒绝：提供“修改问题”，把原问题、场景和仍可用的固定模型恢复到输入区，不原样重发。
- 权限或认证拒绝：不提供无意义重试，提示检查登录、公司范围或联系管理员授权。
- 预算、Prompt 版本、运行治理和内部服务认证：归为系统/治理故障，普通用户查看诊断并联系管理员处理。
- SSE `error.code` 与持久 Run `error_code` 保持一致；Web 同时兼容升级前的异常类名，未知错误仍只允许用户手动重试。

上述自动化已完成，仍需在 staging 人工制造 429、权限拒绝和上游不可用，验收消息内动作、刷新恢复和运行诊断一致性。

### 4.4 常用能力与归档会话

- 常用 Prompt 单击只回填输入框，由用户检查、修改并显式发送。
- 草稿模板后续可以先打开轻量参数表单，再生成完整自然语言请求。
- 归档会话直接禁用 Sender，固定显示只读原因。
- 后续评估“复制上下文并新建会话”和受控恢复会话；不得通过发送后报错来表达只读状态。

## 5. P1：工作台效率、理解和恢复能力

### 5.1 可恢复步骤状态和版本冲突

- 已实现工作台步骤条，明确展示草稿保存、业务校验、正式执行和业务回执，并区分未保存修改、冲突、执行中、已转交、已放弃和已生成回执。
- 页面重新打开后从持久草稿状态和执行回执恢复；同步执行中的瞬时 loading 仍以当前请求为准，不在前端伪造后端中间状态。
- 已实现字段级三方对比、选择性重放本地字段和全部采用最新版本，不再只提示重新打开。
- 冲突恢复以最新持久版本为合并基线；选择后必须再次保存并取得新版本，不能静默覆盖其他页面或其他操作产生的新版本。

### 5.2 草稿卡片业务摘要

已实现共享 `AiDraftCompactSummary`，会话草稿卡片和 `/ai/drafts` 列表在不打开工作台时展示关键业务事实，例如商品建档：

- 商品名称。
- 库存单位。
- 标准售价、批发价、零售价和成本价。
- 初始库存数量和入库仓库。

销售、采购和库存草稿采用各自的紧凑摘要：

- 销售/采购展示往来单位、单据与交付日期、商品行数、按 UOM 分组的数量、草稿金额和涉及仓库；不会把箱、件等不同单位直接相加。
- 订单金额复用共享行金额工具；任一行缺少数量或价格时显示待补充，不把部分行合计伪装为整单金额。
- 库存调整展示商品、仓库、当前库存、目标库存、差异数量、估值参考和原因。
- citation 先由领域 Service 规范化为 `AiDraft`，消息卡片不再自行解析版本、状态、校验和执行回执等草稿元数据；列表直接消费 `listAiDrafts` 返回的同一领域对象，payload 摘要规则集中在共享组件中。

操作层级保持：

- 主操作：复核并执行或完善草稿。
- 次级信息：状态、版本、校验结论和关键业务字段。
- 更多菜单：版本历史、完整业务编辑器和放弃草稿。

危险操作不得与主按钮并列为同等视觉权重。

### 5.3 每条回答对应独立 Run

当前实现为：

- 每条 AI 回答下提供对应的运行详情入口。
- Drawer 精确打开该消息关联的 Run。
- 顶部入口只在生成中显示为“当前运行”。
- 普通用户默认查看状态、耗时、数据范围和错误类别。
- Trace、Token、实际模型和 Provider 诊断收进“高级诊断”。
- 历史消息从会话接口返回的持久 Run 摘要恢复；后端未返回的历史工具明细保持为空，不复用当前或最近 Run 的内存状态。

状态：**已完成，待 staging 人工验收**。

### 5.4 简化场景与模型设置

- 默认只展示“智能模式”和显著的公司上下文。
- 场景、固定模型和其他调试选项进入高级设置。
- 模型优先显示友好名称，技术 alias 只作为补充信息。
- 固定模型能力按角色、策略或明确业务需要开放。

### 5.5 数据新鲜度与完整结果

结构化结果和草稿应显示：

- 查询时间或最近校验时间。
- 当前公司和权限范围。
- 是否截断以及返回数量。
- 刷新当前数据。
- 在业务模块查看完整结果。

商品 citation 中的库存和价格属于“回答时数据”；详情 Drawer 重新读取的数据属于“当前数据”，两者必须使用明确标签区分。

当前实现为：

- 单据结果展示回答时查询或当前刷新时间、公司、当前账号权限范围、返回数量、可见总量和截断状态；无法权限安全计算总量时明确显示未知。
- 每条已完成回答可独立调用 Frappe 只读刷新接口，只替换该消息的业务结果 citation，不创建 Run、不调用模型，也不重新发送 Prompt。
- 每类结果提供业务模块完整列表入口；商品与单据 Drawer 并列展示快照和当前实时读取数据，并可单独刷新。
- 草稿卡片和工作台校验步骤展示持久草稿最近保存并重新校验时间。

状态：**已完成，待 staging 人工验收**。

### 5.6 会话管理

- 已实现服务端关键词搜索，可在本人范围内匹配会话标题和消息正文，并展示最近消息时间。
- 已实现活跃/归档会话重命名；名称修改不改变消息、公司、状态和最近消息排序。
- 已实现每个会话的待复核草稿数量，以及“我的草稿”全局待复核 Badge；执行或放弃草稿后刷新。
- 已实现按会话在当前浏览器内存中保留未发送文本；切换会话或列表状态时分别恢复，发送后清空，归档后删除。
- 后续评估复制上下文新建会话，避免修改归档历史。

状态：**核心效率项已完成，复制上下文新建仍待评估**。

### 5.7 长会话性能

- 已对超长会话采用后端游标分页和 Web 增量加载：首次读取最近 40 条，使用当前最早消息的 `sequence` 作为 `before_sequence` 向前读取，避免 offset 在会话末尾新增消息后重复或跳页。
- 每一页继续返回完整 Run、citation、反馈、失败消息和草稿 citation；Web 前置去重消息并合并反馈，消息级诊断与业务入口继续绑定原消息。
- 加载历史消息不修改 Sender、场景、模型、当前运行或失败恢复状态，并在 DOM 更新后补偿滚动高度，保持用户原来的阅读位置。

状态：**已完成，待 staging 人工验收**。人工验收应覆盖 100 条以上真实会话、连续加载多页、加载后打开旧 Run/citation/草稿、加载失败重试、输入框已有文本和会话正在运行时禁用历史加载。

### 5.8 安全水印可读性

不取消水印，但按风险场景调整：

- 普通工作区降低不透明度并增大间距。
- 避免水印穿过按钮和表单输入区。
- 打印、截图审计和高风险详情保持更强水印。
- 调整前后需要使用长文本、表格和草稿表单做浏览器回归。

## 6. P2：治理后台优化

### 6.1 治理总览和功能页真正拆分

当前模型、策略、用量、向量和审计已经具备独立路由，但策略、用量和审计页面仍复用 `src/pages/Administration/AIModels/index.tsx` 的同一大型实现。目标结构：

- `/administration/ai`：治理总览，只展示跨域 KPI、风险和快捷入口。
- `/administration/ai/models`：模型注册、健康和详情。
- `/administration/ai/policies`：策略生命周期。
- `/administration/ai/usage`：成本、用量和趋势。
- `/administration/ai/vectors`：向量索引和 Embedding 发布。
- `/administration/ai/audit`：审计查询和证据。
- `/administration/ai/data-tasks`：数据治理任务。

独立路由必须对应独立的数据请求、组件边界和页面信息架构，不能只通过 URL 切换同一巨型 Tabs 组件。

### 6.2 策略编辑分步化

策略编辑使用 Steps：

1. 基本信息和业务场景。
2. 主模型与降级链。
3. 限流、并发和预算。
4. 公司、角色和灰度范围。
5. 影响预览、原因和保存。

保存前展示会影响的公司、角色、场景和请求范围，并继续依赖后端验证、审批和发布状态机。

### 6.3 模型同步和可用性检查任务化

- 展示检查模型数量和预计最小请求次数。
- 展示实时进度，以及成功、失败和超时分类。
- 支持只重试失败模型。
- 持久化检查批次和单模型结果。
- 模型详情展示最近成功、连续失败和历史趋势。
- 定时巡检必须具备请求预算、并发上限和通知负责人。
- 自动巡检不得自动停用模型或修改已发布策略。

### 6.4 结构化治理结果

普通管理员优先查看：

- 变更前值和变更后值。
- 影响范围。
- 校验结论和风险等级。
- 执行或回滚结果。
- 责任人和时间。

原始 JSON 仅保留在“技术详情”折叠区，不作为默认业务界面。

### 6.5 可达性、性能和查询缓存

- 建立对话和草稿快捷键、焦点恢复、键盘操作和屏幕阅读器验收矩阵。
- 对 AI 相关大依赖分析 chunk，评估按能力延迟加载。
- 建立首屏、首 Token、长会话内存和交互响应性预算。
- 为模型、用量、审计和数据任务等高频查询增加明确失效条件、手动刷新和可观测缓存策略。

## 7. 工程与文档收口

### 7.1 共享编排与组件拆分

- 抽取共享草稿操作 Hook 或领域控制器，统一工作台和草稿中心的保存、执行、交接、放弃和版本恢复。
- 拆分 `src/pages/AI/index.tsx`、`AiDraftEditorModal.tsx` 和 `Administration/AIModels/index.tsx`，让会话、Sender、草稿、Run 和治理领域分别演进。
- 页面组件只消费领域 Service，不解析后端包络或复制后端状态机。

### 7.2 浏览器 E2E

当前 Jest 覆盖组件和 Service，但尚无完整 Playwright/Cypress 业务流程。E2E 至少覆盖：

- 登录、刷新和 JWT 恢复。
- 发送、停止、失败、重试和刷新恢复。
- 四类草稿编辑、版本冲突、校验、执行和回执。
- 归档会话只读和新建会话。
- 不同角色的治理菜单和危险操作。

测试账号、公司和业务数据必须可重复准备并可清理，不依赖个人浏览器状态。

### 7.3 文档一致性

- 后端 `AI_TECH_DESIGN.zh-CN.md` 中仍有 `product-setup-draft-v1`，应与当前 `product-setup-draft-v2` 契约对齐。
- 统一使用：“模型不能执行正式写入；当前用户明确确认后，由 Frappe 正式领域服务执行。”
- 避免同时使用“AI 不创建正式对象”和“AI 页面原地执行”而不解释执行主体，防止权限边界产生歧义。
- 行为、契约、验证命令或部署假设变化时同步更新设计、开发规范和交接记录。

## 8. 推荐实施顺序

### 第一阶段：验收已交付 P0

1. 使用真实测试账号完成 staging 浏览器验收。
2. 记录 Modal 与右侧大 Drawer 在对话上下文、字段密度和响应式上的真实差异。
3. 验证刷新恢复、重复提交保护、归档只读和失败重试。

### 第二阶段：完成工作台 P1

1. 版本冲突字段对比和可恢复步骤状态（已实现，待 staging 人工验收）。
2. 按错误码分类恢复（已实现，待 staging 人工验收）。
3. 草稿卡片关键业务摘要（已实现，待 staging 人工验收）。
4. 每条消息独立 Run（已实现，待 staging 人工验收）。
5. 数据新鲜度和完整结果入口（已实现，待 staging 人工验收）。
6. 长会话分页或窗口化（已实现，待 staging 人工验收）。

### 第三阶段：降低日常操作负担

1. 会话搜索、重命名、待处理数量和未发送文本保持。
2. 场景和模型迁入高级设置。
3. 水印和长文本可读性优化。

### 第四阶段：治理后台重构

1. 治理总览与功能页真正拆分。
2. 策略分步编辑。
3. 模型检查任务化和历史趋势。
4. 审计及数据治理结果结构化。

### 第五阶段：工程门禁

1. 抽取共享草稿编排。
2. 拆分大型组件。
3. 建立完整浏览器 E2E。
4. 修正文档版本和正式写入主体表述。
5. 完成可达性、性能预算和治理查询缓存。

## 9. 验收门禁

每个阶段至少执行：

```bash
npm run tsc
npm run biome:lint
npm test -- --runInBand
npm run build
npm audit --omit=dev
git diff --check
```

涉及真实业务闭环时，还必须使用 staging 测试账号验证：

- 当前用户权限和公司范围正确。
- 草稿始终执行最新持久版本。
- 校验失败、版本冲突和网络失败不会丢失用户输入。
- 重复点击和页面刷新不会重复创建正式业务对象。
- 正式回执、审计和业务对象可以互相追溯。
- 归档会话、危险治理操作和技术诊断符合角色边界。
